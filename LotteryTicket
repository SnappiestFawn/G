function greenTicket(a, b, c) {
  if (a!=b && b != c && a != c){
    return (0)}
  if (a!=b && b != c && a ==c){
    return(10)}
  if (a!=b && b == c && a !=c){
    return(10)}
  if (a==b && b != c && a !=c){
    return(10)}
  if (a==b && b == c && a ==c){
    return(20)}
}
